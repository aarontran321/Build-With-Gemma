# Pinned for reproducibility (Python 3.11+; built and tested on 3.14).
fastapi==0.141.1
uvicorn[standard]==0.52.0
pydantic==2.13.4
numpy==2.5.1
ollama==0.6.2
pytest==9.1.1
httpx==0.28.1
# Server-side PDF export for mission reports. Pure-Python wheel on purpose:
# no Cairo/Pango/Homebrew system libraries and no headless browser, so
# report export works on a judging machine that is fully offline.
reportlab==5.0.0
